{
  "dependencies": {
    "axios": "^1.7.2",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "constructs": "^10.4.2",
    "esbuild": "^0.24.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  },
  "scripts": {
    "render-postbuild": "npm install --prefix my-react-app vite && npm run build --prefix my-react-app",
    "install": "npm install --prefix my-react-app && npm --prefix backend install backend && npm --prefix my-react-app install my-react-app",
    "dev:backend": "npm install --prefix backend start",
    "dev:frontend": "npm install --prefix my-react-app start",
    "sequelize": "npm run --prefix backend sequelize",
    "sequelize-cli": "npm run --prefix backend sequelize-cli",
    "start": "npm start --prefix backend",
    "build": "npm run build --prefix backend"
  }
}
